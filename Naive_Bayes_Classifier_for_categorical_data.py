
#The alroghrim is basically counting to calculate required probability distribution for P(Y|X)

# 1. Calculate the class probability: P(Y = y_i)
# 2. Calculate the conditional probability(ies): P(X = x_i| Y = y_i)
# 3. Time each P(X = x_i| Y = y_i)* P(Y = y_i)
